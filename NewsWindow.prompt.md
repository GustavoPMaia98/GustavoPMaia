import * as React from 'react';

export interface TagProps {
  /** Chip label. */
  children?: React.ReactNode;
}

/** A cyan-tinted keyword chip for research topics and tags. */
export function Tag(props: TagProps): React.ReactElement;
